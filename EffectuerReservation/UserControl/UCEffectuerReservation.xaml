<UserControl
    x:Class="SAE2._01_Loxam.FicheClients.UserControls.UCEffectuerReservation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SAE2._01_Loxam.FicheClients.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="686"
    d:DesignWidth="1040"
    mc:Ignorable="d">

    <!--  Contenu principal  -->
    <StackPanel>

        <!--  Titre + bouton rechercher  -->
        <StackPanel
            Margin="0,0,0,20"
            HorizontalAlignment="Stretch"
            Orientation="Horizontal">
            <TextBlock
                VerticalAlignment="Center"
                FontSize="22"
                FontWeight="Bold"
                Text="Effectuer une réservation" />
            <Button
                Width="300"
                Height="50"
                Margin="20,0,0,0"
                HorizontalAlignment="Right"
                Background="Red"
                Content="RECHERCHER UNE RÉSERVATION"
                FontWeight="Bold"
                Foreground="White" />
        </StackPanel>

        <!--  Bloc Disponibilités  -->
        <Border
            Padding="20"
            BorderBrush="LightGray"
            BorderThickness="1"
            CornerRadius="10">

            <StackPanel>

                <TextBlock
                    Margin="0,0,0,10"
                    FontSize="20"
                    FontWeight="Bold"
                    Text="Disponibilités" />

                <!--  DataGrid  -->
                <DataGrid
                    x:Name="DataGridDisponibilites"
                    Margin="0,10,0,0"
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    ColumnHeaderHeight="50"
                    FontSize="16"
                    HeadersVisibility="Column"
                    IsReadOnly="True"
                    ItemsSource="{Binding ListeDisponibilites}"
                    RowHeight="50">

                    <DataGrid.Columns>

                        <DataGridTextColumn
                            Width="200"
                            Binding="{Binding Materiel}"
                            Header="Matériel" />
                        <DataGridTextColumn
                            Width="150"
                            Binding="{Binding Lieu}"
                            Header="Lieu" />
                        <DataGridTextColumn
                            Width="150"
                            Binding="{Binding DateDebut}"
                            Header="Du" />
                        <DataGridTextColumn
                            Width="150"
                            Binding="{Binding DateFin}"
                            Header="Au" />

                        <!--  Etat avec bindings  -->
                        <DataGridTemplateColumn Width="150" Header="État">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border
                                        Padding="5"
                                        Background="{Binding CouleurEtat}"
                                        CornerRadius="5">
                                        <TextBlock
                                            HorizontalAlignment="Center"
                                            FontWeight="SemiBold"
                                            Text="{Binding Etat}" />
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!--  Voir détails  -->
                        <DataGridTemplateColumn Width="120" Header="">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button
                                        Width="100"
                                        Height="30"
                                        Margin="5"
                                        Command="{Binding CommandVoirDetails}"
                                        Content="👁 Voir détails" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!--  Réserver  -->
                        <DataGridTemplateColumn Width="120" Header="">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button
                                        Width="100"
                                        Height="30"
                                        Margin="5"
                                        Background="Red"
                                        Command="{Binding CommandReserver}"
                                        Content="RÉSERVER"
                                        Foreground="White" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

    </StackPanel>
</UserControl>